# Assets

Store only original or appropriately licensed project assets here, organized into `tiles`, `characters`, `monsters`, `items`, `effects`, and `audio` as they are introduced.

Gameplay and content data must refer to stable asset IDs such as `monster.wolf`, never embed repository paths as domain rules.
