# @eldoria/web

React, Vite, TypeScript, and Phaser browser client. It renders presentation state and sends intent; authoritative gameplay rules do not belong here.

```bash
pnpm --filter @eldoria/web dev
```

Set `VITE_GAME_SERVER_URL` to override the default `ws://<browser-host>:8787` game server address.
