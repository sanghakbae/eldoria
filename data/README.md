# Gameplay data

Data-driven content belongs in subdirectories for `items`, `monsters`, `npcs`, `recipes`, `skills`, `zones`, and `loot-tables`. Later tasks must add schemas and validate all data at server startup.
